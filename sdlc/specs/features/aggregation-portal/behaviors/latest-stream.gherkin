# 页面: pc-portal-home（门户首页 · 最新流区块）
Feature: 首页最新内容流（同仓小源）
  作为技术读者，我要在门户首页看到同仓小源的最新若干条 digest 倒序流，
  以便不点进子站也能瞥见最新更新。

  Rule: 最新流按时间倒序聚合同仓小源

    @happy-path
    Scenario: SC-08 最新流区块按时间倒序列同仓小源 digest
      Given 门户首页第三区块（US-01 已占位）
      And 同仓存在小源 digest（如 claude-blog）
      When 首页构建生成最新流区块
      Then 按发布时间倒序列出同仓小源最新若干条 digest

    @boundary
    Scenario: SC-09 最新流不含 github-trending 明细
      Given 首页构建期拿不到 github-trending 另一仓内容
      When 生成最新流区块
      Then 区块不含 github-trending 的最新明细

    @boundary
    Scenario: SC-10 无同仓小源内容时区块优雅留空不报错
      Given 同仓当前无可用小源 digest
      When 首页构建生成最新流区块
      Then 区块优雅留空或隐藏
      And 构建不报错、首页其余区块正常渲染
