# 页面: 小源子站（/claude-blog/*，同仓 Jekyll 渲染）
Feature: 小源同仓分目录子站
  作为技术读者，我要点小源卡（如 Claude Blog）进入 /claude-blog/ 等子目录，
  看到该源完整内容，以便阅读单源的全部译读/深读文章。

  Rule: 小源子站由本仓 Jekyll 在子路径下正确渲染

    @happy-path
    Scenario: SC-11 子路径由本仓 Jekyll 渲染且样式/链接不错位
      Given 本仓 Jekyll baseurl 对小源子站设为对应路径（如 /claude-blog）
      When 访问 /claude-blog/
      Then 由本仓 Jekyll 渲染该源内容
      And CSS 与内部链接在 /claude-blog/ 前缀下不错位

    Scenario: SC-12 子站内相对路径在前缀下正确解析
      Given 处于 /claude-blog/ 子站
      When 加载页面资源并点击内部链接
      Then 页面 CSS 加载返回 200
      And 内部链接可点达对应页面

    @boundary
    Scenario: SC-13 同仓小源直出、不经 Worker 路由
      Given / 与 /claude-blog/ 同属本仓 Pages
      When 分别访问 / 与 /claude-blog/
      Then 二者均可达
      And Worker 路由仅对 /github-trending/* 生效，不介入同仓路径

    @error
    Scenario: SC-23 baseurl 未设/配错时视为配置错误（不带病上线）
      Given 小源子站 Jekyll baseurl 未设为其挂载路径
      When 访问该子站页面
      Then 该子站页面的 CSS/内链请求返回 404（前缀错位，INV-01 违反的可观测症状）
      And 部署前的构建/校验步骤检测到该错位并以非零退出码失败
